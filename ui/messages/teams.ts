import { Scrim } from "@prisma/client";
import { Team, TextChannel } from "discord.js";

export async function sendTeamDetails(
  channel: TextChannel,
  scrim: Scrim,
  team: Team,
) {
  // TODO: use this
  // Placeholder function to send team details to a specified channel
  // Implementation would involve creating an embed with team details
  // and sending it to the provided channel
}
